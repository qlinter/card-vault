# v1.3.0 开发进度与验收

更新日期：2026-09-07。本文按最终范围维护当前状态，取代逐次追加的界面修改日志。功能细节见[文档索引](./README.md)，版本摘要见[更新说明](./release-v1.3.0.md)。

## 当前状态

v1.3.0 本地功能、已确认界面调整、代码整理与当前源码验收已完成。已经用户确认重新生成 Windows x64 安装包、便携包及 SHA-256 清单，包内运行和产物验证全部通过。

用户约定：先完成审核、修复、文档同步与验证，再由用户确认是否生成 v1.3.0 安装包和便携包。代码修改、版本号和源码检查通过不构成打包授权。

## 功能完成情况

| 范围 | 状态 | 当前实现 |
| --- | --- | --- |
| 财务统一 | 已实现 | 实物数量、双币付款、人工汇率、统一报表和冻结快照。 |
| 数据可靠性 | 已实现 | 共享操作锁、清理恢复副本、备份清单、暂存恢复、日志降级。 |
| 国际化与编辑 | 已实现 | 显式界面翻译保护用户内容，失败后保留页面内输入。 |
| 大收藏量 | 已实现 | 首页分页、报表索引、稳定财务排序、组合分块和合成规模基准。 |
| 数据 | 已实现 | 设置内集中管理存储、备份恢复、导入和导出，预演、重试与撤销。 |
| 计划 | 已实现 | 180 天提醒、7/30 天摘要、独立折叠心愿表单/列表与预算。 |
| 分享与界面 | 已实现 | 统一预览/导出，紧凑视图、精简标题、并列分享操作、统一路径字体。 |
| 代码与文档整合 | 已完成 | 退役接口清理、共享工具合并、导出查询优化、规范与进度收敛。 |
| 安装包与便携 ZIP | 已完成 | 经用户确认构建，包内卡片/分享/管理与健康端点、版本、签名模式及 SHA-256 验证通过。 |
| 长期在线发布与同步 | 暂缓 | 不阻碍本地版本收口，未来需重新确认范围和资源。 |

独立批量修改界面、最近批次列表、系统通知、分享“更多”菜单、各栏目单独说明入口均已取消，不是待办。批次历史数据与旧通知字段保留兼容；不因清理代码而删除用户历史。

## 当前源码验收

2026-09-07 在 Windows、Node.js 24.14.0 环境完成，日志位于本机忽略目录 logs/v130-consolidation。

| 检查 | 最新结果 |
| --- | --- |
| 完整源码门槛 | npm run check:release 通过（release-check-final.log），不含打包。 |
| 单元与模块测试 | 239 项通过，0 失败；含文件分块校验、备份清单完整性和表格输入边界回归。 |
| 覆盖率 | 实际加载模块行 93.26%、分支 80.99%、函数 92.34%；不等同于所有页面和 Electron 启动路径的覆盖率。 |
| HTTP 流程 | 本地会话安全、卡片/财务/队列、分享预览导出、数据导入/重试/撤销/计划/分页全部通过；含旧估值批次兼容及退役接口拒绝。 |
| UI 回归 | 65 项通过：62 项桌面、3 项分享网页模拟手机视口；16 张严格视觉比较通过，未更新或放宽基线。 |
| 构建与静态检查 | 生产构建、UTF-8、Lint、TypeScript、文档链接、版本及源码误忽略检查通过。 |
| 依赖审计 | 生产依赖及更新后的全部依赖均为 0 个已知漏洞；fast-uri 3.1.7、@xmldom/xmldom 0.8.15，CI 同步完整审计。 |
| 容量复测 | 1k/5k/10k 合成收藏的分页、排序和组合读取通过；数值与限制见[数据与计划](./data-center-guide.md)。 |

关键整理包括修复 Git 忽略规则漏掉设置数据源码的问题、合并数据工作区与文件哈希、清理退役接口和失效样式、优化导出查询、加固备份清单、整合全部 24 份 Markdown 与应用内说明。历史发布说明按归档处理，现行文档不沿用旧测试数字或已删除产物的校验值。

所有数据变更测试使用隔离数据库。随后经用户确认执行完整发布流程，重复的源码门槛仍全部通过；本次未签名 Windows x64 产物及校验值已登记到[发布说明](./release-v1.3.0.md)。未执行用户环境安装或线上上传。

## 打包后验收（已完成）

- [x] 用户于 2026-09-07 明确确认生成安装包和便携包。
- [x] 执行 `npm run release:win`，检查包内卡片/分享/数据管理与健康端点。
- [x] 验证安装器、ZIP 文件、版本、签名模式及 SHA-256。
- [x] 更新分发文件大小、校验值和发布状态。

全新 Windows 覆盖安装、真实签名、密集历史/大媒体、磁盘耗尽和断电恢复不因自动测试通过而视为完成；真实 AI 服务没有在本轮使用用户密钥调用。
