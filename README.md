# Card Vault

Card Vault 是一个本地优先的球星卡收藏管理工具，用于录入、筛选、展示和桌面端运行个人收藏数据。

项目使用 `Next.js + React + Prisma + SQLite + Electron` 搭建。卡片数据和图片默认保存在本地，适合离线使用，也支持在桌面端切换数据存储路径。

## 当前版本

- `1.0.5`

## 版本说明

### 1.0.0 基础版本功能

`1.0.0` 是项目的首个完整可用版本，具备本地收藏管理和桌面端运行所需的核心能力。

- 支持新增、编辑、删除球星卡。
- 支持上传卡片图片并进行本地保存。
- 支持首页按关键词、运动类型、球队、年份、产品线、签名卡、评级状态等条件筛选。
- 支持按录入时间、年份、价格、评级排序。
- 支持卡片详情页查看图片、基础信息、购买信息、评级信息和备注。
- 支持“展示”浏览全部卡片，并进入单卡展示详情。
- 支持 Electron 桌面端运行。
- 支持本地 SQLite 数据库存储。
- 支持本地图片目录与数据库分离保存。
- 支持切换桌面端数据存储路径。
- 支持生成 Windows 安装版和便携版发布包。

### 1.0.1 升级内容

`1.0.1` 在 `1.0.0` 基础上，加强了展示体验、录入稳定性、筛选能力和中文文档质量。

- “展示”增加球员名列表的展开/收起功能。
- “展示”左上角增加统计文案，显示“当前展示 X 张卡片，来自 Y 位球员或组合”。
- “展示”球员分组中显示每位球员或组合对应的卡片数量。
- “展示”单卡详情保留卡片级切换按钮，并使用英文 `Previous / Next` 区分图片切换。
- 首页增加卡片数量显示，并保留总估值统计。
- 新增录入页在提交失败后保留已输入的文字和勾选项。
- 单张卡片图片上限统一为 5 张，并支持新增时一次选择多张图片上传。
- 年份支持自由输入，例如 `2016-17`。
- “评级机构 / 评级”支持自由输入，适配非纯数字评级场景。
- 新增录入页去掉“是否编号卡”勾选项。
- 首页筛选去掉“编号卡”，改为 `Patch/Jersey` 筛选。
- 清理并统一页面、桌面端提示和文档中的中文文案。

### 1.0.2 升级内容

`1.0.2` 主要补充投入成本相关字段，并修复新增卡片后的成功提示问题。

- 新增“评级费用”字段。
- 新增“总投入”字段。
- 录入和编辑卡片时自动计算“总投入 = 购买价格 + 评级费用”。
- 后端保存时会重新计算总投入，减少前端输入误差。
- 卡片详情页在“购买价格”和“当前估值”之间展示“评级费用”和“总投入”。
- 修复新增卡片成功后误显示 `NEXT_REDIRECT` 的问题，改为正常显示“添加成功”。
- 桌面端安装包和打包版本使用应用图标，并在打包过程中额外写入 Windows 可执行文件图标资源。
- 本地 SQLite 初始化和迁移脚本已支持新增字段。

### 1.0.3 升级内容

`1.0.3` 主要围绕数据结构扩展、首页检索能力和后续分享/展馆能力的基础字段进行升级。

- 扩展卡片数据字段，新增品牌、产品线、子系列、平行版本、Rookie、签字类型、Patch 类型、证书号、公开状态、收藏状态等信息。
- 删除与“产品线”重复的“系列”字段，旧数据库中的“系列”数据会在迁移时优先合并到“产品线”。
- 公开状态补充为“私密 / 公开 / 仅链接可见”，为后续分享页和云端展馆功能预留权限基础。
- 首页搜索关键词覆盖更多字段，包括品牌、产品线、子系列、平行版本、编号、评级机构、评级、证书号、签字类型、Patch 类型、购买渠道、标签、描述和备注。
- 首页筛选栏改为“常用筛选 + 更多筛选”结构，更多筛选支持展开/隐藏。
- 更多筛选新增品牌、子系列、平行版本、卡号、编号、编号范围、Rookie、签名卡、签字类型、Patch/Jersey、Patch 类型、已评级、评级机构、评级、证书号、公开状态、收藏状态。
- 当使用更多筛选条件后，页面刷新会自动保持展开状态，方便确认当前筛选条件。
- 编辑卡片时同步保存扩展字段，避免新增字段只在录入时生效、编辑时丢失。
- 数据库初始化和迁移脚本兼容旧数据，会自动补充新增字段并处理“系列”到“产品线”的迁移。
- 继续清理中文乱码风险，对本次涉及的首页和筛选组件做了额外检查。

### 1.0.4 升级内容

`1.0.4` 主要围绕 AI 识图录入、全局设置入口和跨电脑维护稳定性进行升级。

- 新增顶部“设置”入口，提供全局配置页，为后续非 AI 设置预留空间。
- AI 设置迁移到全局设置页，新建卡片和编辑卡片共用同一套模型配置。
- AI 服务商支持 Azure OpenAI 和 MiniMax，二者的 Endpoint、API Key、模型 / 部署配置独立保存，切换服务商不会互相覆盖。
- 新增“读取模型”能力，可从当前服务商读取模型或部署列表，并支持使用当前界面配置测试连接。
- 新增卡片时支持使用 1-2 张正反面图片生成录入建议。
- 编辑已有卡片时支持使用当前卡片已有默认图片进行 AI 填充，也可重新选择图片识别。
- AI 填充默认不覆盖已有字段；需要刷新已有信息时可手动勾选“覆盖当前字段”。
- AI 生成展示描述时改为中文，侧重球星生涯、代表性成就和卡片收藏意义；备注字段保持为空。
- 加强 MiniMax 返回内容解析，兼容非严格 JSON、多段 content 和 JSON 修复兜底。
- 增加编码维护护栏，统一 UTF-8 / LF，并在构建前执行编码检查，降低跨电脑同步乱码风险。
- `npm install` 后自动执行 Prisma Client 生成，改善新电脑同步开发体验。

### 1.0.5 升级内容

`1.0.5` 主要围绕分享集、静态展馆导出、页面视觉统一和产品命名收束进行升级。

- 新增顶部“分享”入口，用于创建面向他人的球星卡精品展馆。
- 新增分享集数据模型，支持分享集标题、副标题、简介、封面图、主题文案、卡片排序和单卡展示覆盖。
- 分享集默认由用户手动挑选卡片，导出时采用公开字段白名单，不包含购买价格、评级费用、总投入、当前估值、购买渠道、备注、AI Key 或本地数据库路径。
- 分享集创建页改为四步向导：选择球星卡、AI 生成、内容修改、确认保存，减少长页面操作负担。
- 选择球星卡步骤支持搜索，并按运动类型分组折叠，方便大量卡片下快速挑选。
- 分享集封面支持自动使用第一张有图卡片，也支持自定义上传；自定义封面保存在独立的 `share-covers` 目录，便于和卡片图片分开管理。
- 分享集支持调用当前全局 AI 设置生成中文展馆文案，AI 结果只填入草稿，用户可继续修改后保存。
- 分享向导改为状态驱动，减少 DOM 查询依赖，提升选卡、排序、AI 填表和保存流程的稳定性。
- 分享预览支持在多张卡片间切换大图查看。
- 新增静态导出和云端发布包生成能力；云端发布包使用通用服务器静态托管描述，不绑定具体云厂商。
- 云端发布包会生成 `README-deploy-cloud.md` 和 `nginx-card-vault-share.conf` 示例配置，后续可扩展为 SSH/SFTP 一键上传。
- AI 返回内容解析抽为共享逻辑，识图录入和分享主题生成共用 JSON 提取、thinking 内容清理和多结构 response 兼容处理。
- Azure OpenAI、MiniMax 的分享主题生成修复了空字段 `.trim()` 和非严格 JSON 解析问题。
- 将产品导航中的“展示页”统一命名为“展示”，分享预览/导出中的 `Card Vault Share` 统一为 `Card Vault 展馆`。
- 删除产品界面和文档中的特定云厂商品牌字样，统一为“云端发布 / 服务器静态托管”。
- 设置页中的“AI 识图设置”改为“AI 设置”，并默认收起。
- 统一按钮视觉，区分主操作、次操作和危险操作。
- 普通产品页面统一为简洁明朗的浅色产品风格，展示页、分享页、设置页和首页使用同一套明亮背景图风格。
- 首页、展示、分享、设置分别使用独立背景图资源：`home-bg.png`、`showcase-bg.png`、`shares-bg.png`、`settings-bg.png`。
- 桌面窗口标题和打包产品名统一为 `Card Vault`。
- 增加本地缓存清理脚本和 TypeScript 检查脚本，构建前会同时检查编码和类型。
- 清理打包配置中的 `.env` 项，避免本地环境配置进入桌面端发布包。

## 主要功能

- 新增、编辑、删除球星卡。
- 单张卡片最多上传 5 张图片。
- 支持 AI 识图录入和已有卡片 AI 填充，可用 1-2 张正反面图片生成字段建议，当前支持 Azure OpenAI 和 MiniMax。
- 支持创建分享集，手动挑选卡片生成精品展馆。
- 支持分享集 AI 主题生成，可生成中文展馆标题、封面介绍、收藏叙事和分组说明。
- 支持静态导出和服务器静态托管预留，导出包不依赖本地数据库。
- 支持自由输入年份，例如 `2016-17`。
- 支持自由输入评级，例如 `9.5`、`Auto Auth`、`Authentic`。
- 支持购买价格、评级费用、总投入、当前估值记录。
- 支持品牌、产品线、子系列、平行版本、Rookie、签字类型、Patch 类型、证书号、公开状态、收藏状态等扩展字段。
- 首页支持常用筛选和可展开的更多筛选，覆盖更多卡片字段。
- 首页筛选、排序、搜索和数量统计。
- “展示”浏览球员分组和单卡详情。
- 本地 SQLite 数据库存储。
- Electron 桌面端运行。
- 支持迁移和备份本地数据目录。

## 技术栈

- `Next.js 15`
- `React 19`
- `TypeScript`
- `Prisma`
- `SQLite`
- `Electron`
- `Tailwind CSS`

## 项目结构

- `app/`：Next.js App Router 页面、详情页、“展示”、Server Actions。
- `components/`：表单、筛选栏、展示图库、桌面端存储设置等组件。
- `lib/`：Prisma 访问、筛选逻辑、展示逻辑、图片路径和存储解析。
- `electron/`：Electron 主进程、预加载脚本、本地存储迁移逻辑。
- `scripts/`：初始化数据库、准备本地构建、图标生成等脚本。
- `prisma/`：Prisma schema 和本地 SQLite 数据库定义。
- `public/`：静态资源。

## 本地运行

### 1. 安装依赖

```bash
npm install
```

### 2. 生成 Prisma Client

```bash
npm run prisma:generate
```

### 3. 初始化本地数据库

```bash
npm run db:init
```

### 4. 构建 Next.js 应用

```bash
npm run build
```

### 5. 启动桌面端

```bash
npm run electron
```

也可以在 Windows 下直接运行：

```bat
start-desktop.bat
```

说明：使用 `start-desktop.bat` 属于开发态启动方式，任务栏图标可能仍受 Electron 开发运行方式影响。正式安装版和打包版会使用应用图标。

## 发布包说明

项目支持两种 Windows 发布形式，均可在第三方电脑上直接使用，不需要目标电脑再执行 `npm install`。

### 安装版

文件：

- `dist/card-vault-1.0.5-setup.exe`

特点：

- 通过安装向导完成安装。
- 可选择安装目录。
- 更适合长期在同一台电脑上使用。
- 安装完成后可像普通 Windows 软件一样启动。

### 便携版

文件：

- `dist/card-vault-1.0.5-portable.zip`

使用方式：

1. 解压压缩包。
2. 进入解压后的目录。
3. 运行 `Card Vault.exe`。

特点：

- 不需要安装。
- 可放在任意文件夹、移动硬盘或 U 盘中运行。
- 更适合测试、临时使用或快速分发。

### 安装版和便携版的区别

- 安装版适合长期正式使用，便携版适合免安装运行。
- 安装版通过 `setup.exe` 安装，便携版通过解压后直接运行。
- 两者都可以独立运行，不需要目标电脑安装 Node.js 或项目依赖。

说明：

- 当前发布包未进行代码签名，部分 Windows 电脑首次运行时可能出现安全提示。
- 这是未签名桌面应用的常见现象，不影响程序本身运行。

## 常用脚本

- `npm run build`：构建 Next.js 应用。
- `npm run check:encoding`：检查源码是否存在典型中文乱码或非法 UTF-8 内容。
- `npm run typecheck`：执行 TypeScript 类型检查。
- `npm run clean:cache`：清理 `.next/cache`、`logs` 和 `tsconfig.tsbuildinfo` 等可再生成缓存。
- `npm run start`：启动 Next.js 生产服务。
- `npm run electron`：启动 Electron 桌面端。
- `npm run prepare:local`：准备本地桌面运行所需构建。
- `npm run db:init`：初始化或迁移本地数据库。
- `npm run prisma:generate`：生成 Prisma Client。
- `npm run dist:win`：构建 Windows 安装版和发布目录。

## AI 识图录入

顶部“设置”页提供全局 AI 设置；新增卡片和编辑卡片页面提供 AI 识图录入 / 填充入口。当前支持 Azure OpenAI 和 MiniMax。

- 桌面端会把 AI 设置保存到本机用户数据目录中的 `ai-config.json`，不会写入 Git 仓库或卡片数据库。
- Azure OpenAI 设置项包括 Endpoint、API Key、Deployment 和 API Version。
- MiniMax 设置项包括 API Endpoint、API Key 和 Model。
- 开发态可以通过 `.env.local` 配置 `CARD_VAULT_AI_PROVIDER`，并按服务商填写 `AZURE_OPENAI_*` 或 `MINIMAX_*`。
- 识图录入支持 1-2 张 `jpg/png/webp` 图片，适合上传卡片正面和背面。
- 已录入卡片可在编辑页使用已有默认图片进行 AI 填充。
- AI 结果只作为录入建议填入表单，用户可以继续修改后再保存。
- 默认只填空字段，勾选“覆盖当前字段”后才会替换已有输入。

## 分享与云端发布

顶部“分享”用于创建面向他人的分享集。分享集独立于本地“展示”，默认由用户手动挑选卡片。

- 分享集支持标题、副标题、封面介绍、展馆叙事、收藏亮点、主题分组和封面图。
- 分享集可调用当前全局 AI 设置生成中文展馆文案，AI 结果只填入草稿，用户可继续修改。
- 分享集封面支持自动使用第一张有图卡片，也支持自定义上传；自定义封面保存在独立的 `share-covers` 目录，便于和卡片图片分开管理。
- 分享集支持为单张卡设置展示标题和展示描述覆盖，不影响原始卡片数据。
- 静态导出会生成完整 HTML/CSS/图片/data 文件夹和 zip。
- 云端发布包会额外生成 `README-deploy-cloud.md` 和 `nginx-card-vault-share.conf` 示例配置，适合上传到服务器静态目录。
- 导出包使用相对路径，可本地打开，也可部署到服务器、静态网站或其他静态托管。
- 导出内容采用脱敏白名单，不包含购买价格、评级费用、总投入、当前估值、购买渠道、备注、AI Key 或本地数据库路径。

## 数据存储

项目是本地优先设计。

- 卡片元数据保存在 SQLite 数据库中。
- 卡片图片保存在本地 `uploads` 目录中。
- 分享集自定义封面保存在本地 `share-covers` 目录中。
- 桌面端支持切换“存储路径”。

开发态默认数据库位置：

```text
prisma/dev.db
```

桌面端实际运行时，数据通常会使用独立的数据目录，而不是项目根目录里的数据库文件。

详细说明见：

- [数据备份说明](./数据备份说明.md)

## GitHub 仓库与本地运行说明

以下目录或文件通常不建议提交到 GitHub：

- `.env`
- `node_modules`
- `.next`
- `dist`
- `data`
- 本地数据库文件

原因是它们属于本地配置、依赖安装结果、构建产物或个人数据，不适合作为源码仓库的一部分。

但要注意：这些文件虽然通常不提交到 GitHub，对“当前这份本地可直接运行的工作目录”来说，其中一部分是运行必需项。例如 `node_modules` 和 `.next` 如果被删除，本地桌面端就无法直接启动，必须重新安装依赖并重新构建。

因此建议区分两种使用方式：

- GitHub 仓库用于保存源码、文档和配置。
- 安装版 / 便携版用于给第三方电脑直接运行。

## 项目体积与阶段维护

当前开发目录体积主要来自 `node_modules`，这是 Electron、Next.js、Prisma、TypeScript 等依赖安装后的实际文件，不属于源码本身。`node_modules` 通常不提交到 GitHub，但保留它可以让本机继续直接开发运行。

阶段性清理原则：

- 可安全清理：`.next/cache`、`logs`、`tsconfig.tsbuildinfo`、旧的 `dist` 构建产物。
- 谨慎清理：完整 `.next` 会影响生产启动，需要重新 `npm run build`。
- 不建议随手清理：`node_modules`，删除后必须重新执行 `npm install`。
- 不要清理或提交：本地数据库、上传图片、AI Key、`.env` / `.env.local`。

推荐阶段检查流程：

```bash
npm run clean:cache
npm run check:encoding
npm run typecheck
npm run build
```

本次阶段整理后，分享功能仍在 `sharing-feature` 分支继续开发；合并回 `master` 前建议重点回归分享集创建、AI 主题生成、静态导出、云端发布包、新增 / 编辑卡片流程和各顶层页面背景显示效果。

## 跨电脑同步与编码约定

项目源码统一使用 `UTF-8` 编码，并通过 `.editorconfig` 和 `.gitattributes` 固定常见文本文件的编码与换行规则。

在不同电脑之间通过 GitHub 同步源码时，建议：

- 使用支持 `.editorconfig` 的编辑器，并确认文件以 `UTF-8` 打开和保存。
- 不要用 GBK、ANSI 或系统默认编码重新保存 `.ts`、`.tsx`、`.js`、`.md` 等源码文件。
- 提交前运行 `npm run check:encoding`，检查源码是否出现典型中文乱码或非法 UTF-8 内容。
- 收藏数据仍然按“数据备份说明”单独迁移，不要提交本地数据库、图片、构建产物或日志。

## 后续规划方向

下一阶段建议优先考虑：

- 服务器 + SSH/SFTP 一键发布。
- 主题展馆和收藏家主页。
- AI 识别录入的准确率优化、更多识别场景和字段置信度提示。
- 手动成交参考与 AI 估值解释。

## 说明

这是一个偏本地使用场景的项目，桌面端和本地数据目录的逻辑比普通 Web 项目更多。如果后续要公开到 GitHub，建议保留本 README 和 [数据备份说明](./数据备份说明.md)，方便他人理解项目定位与使用方式。
