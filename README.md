# Card Vault

简体中文 | [English](./README.en.md)

Card Vault 是一款本地优先的球星卡收藏管理工具，用于录入、整理、检索、展示和分享个人收藏。项目基于 `Next.js + React + Prisma + SQLite + Electron`，卡片数据与图片默认保存在本机，可离线使用。

## 当前版本

`1.0.17`

### 1.0.17 重点更新

- AI 设置支持新增、命名和保存多套 OpenAI Chat Completions 兼容服务；列表直接显示用户设定名称，并允许本地无鉴权服务留空 API Key。
- 加固 Azure OpenAI、MiniMax 和自定义 AI 请求：限制组合分析输入体积，兼容不同输出参数，自动重试瞬时错误、JSON 模式和模型参数差异。
- 组合分析升级为统一五维报告，覆盖组合结构、财务记录、收藏特征、估值时效和档案完整度；补齐判断、证据、关注事项和整理行动建议。
- 当远程 AI 无法返回完整协议时，自动补全内容或展示基于本地确定性统计生成的完整保底报告，避免只显示综合评分或整页失败。
- 将卡片字段统一显示为 `Team`，兼容足球俱乐部、篮球队与 F1 车队等不同场景。
- 财务历史取消迁移、纠错、人工录入等编辑状态文案，将单条记录操作统一为“编辑”，并保持 CNY/USD 历史事实不变。
- 修复筛选后编辑卡片或财务历史再返回列表时丢失筛选、排序和分页上下文的问题。
- 统一 AI 配置规范化、组合分析协议、图片路由、反馈消息和分享卡片映射，清理无引用旧样式与冗余兼容代码。
- v1.0.17 不新增数据库迁移，保持 v1.0.16 数据、备份与恢复流程直接兼容。

## 核心功能

- 球星卡新增、编辑、删除、详情查看和最多 5 张图片管理。
- 按球员、运动、Team、年份、产品线、评级、签名、Patch、状态等字段搜索、筛选和排序。
- 以交易、费用和估值历史记录购入、售出、退款、评级及其他成本，并维护最新估值。
- 展示页按球员或组合浏览收藏，支持分组收起、卡片切换和单卡多图切换。
- 使用 Azure OpenAI、MiniMax 或 OpenAI Chat Completions 兼容的自定义 AI 进行识图录入、分享文案生成和组合分析。
- 创建可编辑的分享展馆，配置主题、布局、章节、封面、背景和单卡展示内容。
- 导出通用静态分享包或 Cloudflare Drop 临时预览包。
- 本地 SQLite 存储、可切换数据路径、独立备份路径、健康检查和恢复。
- 提供 Windows 安装版与便携 ZIP，目标电脑无需安装 Node.js。

## 版本简史

| 版本 | 主要变化 |
| --- | --- |
| `1.0.0` | 完成本地卡片管理、筛选、展示、SQLite 存储和 Electron 桌面运行。 |
| `1.0.1` | 增加多图上传、展示统计与分组收起，改善录入失败后的信息保留。 |
| `1.0.2` | 增加评级费用与总投入，修复新增成功提示并完善桌面图标。 |
| `1.0.3` | 扩展卡片字段、公开状态和高级搜索，并兼容旧数据库。 |
| `1.0.4` | 增加 Azure OpenAI / MiniMax AI 识图录入、统一设置与编码检查。 |
| `1.0.5` | 增加分享集、四步创建向导、AI 展馆文案和静态导出。 |
| `1.0.6` | 将存储路径迁入设置页，增加独立备份路径与一键备份。 |
| `1.0.7` | 增加分享背景、单卡展示覆盖、编辑能力和 3D 图片切换。 |
| `1.0.8` | 加固存储迁移、图片校验、SQLite 备份、桌面启动和自动化测试。 |
| `1.0.9` | 建立通用、运动与 Team 主题系统，统一预览和导出视觉资源。 |
| `1.0.10` | 增加三套展馆布局、可排序章节、实时预览和统一渲染器。 |
| `1.0.11` | 完成迁移、恢复、密钥安全、数据健康、组合分析和发布验证基础。 |
| `1.0.12` | 升级核心依赖，加固存储迁移，完善安全导航、筛选上下文与分享展馆编辑器 2.0。 |
| `1.0.13` | Azure OpenAI 统一迁移到 v1 API，支持统一资源 Endpoint 与 GPT-5.4 / 5.5 / 5.6 系列。 |
| `1.0.14` | 完成分享编辑器 2.0 与 Drop 导出收口，建立可靠财务历史、历史化组合分析、备份自动迁移及可选 Windows 发布签名。 |
| `1.0.15` | 完成新版 AI 组合分析协议、Electron/存储/分享编辑器进一步拆分、反馈消息统一、路径规则收敛和 Tailwind 清理。 |
| `1.0.16` | 收敛首页历史查询，建立 Windows 干净环境质量检查、可重复发布候选流程、产物校验和版本元数据保护。 |
| `1.0.17` | 支持多套自定义 AI，提升组合分析成功率并统一五维报告，修复筛选返回上下文，同时完成共享协议与冗余代码整理。 |

## 安装与运行

### 安装版

发布文件：`dist/card-vault-1.0.17-setup.exe`

- 使用安装向导，可选择安装目录，适合长期使用。
- 安装较早版本后可直接安装同一应用的新版本，通常会覆盖程序文件，不会主动删除收藏数据。
- 未配置代码签名证书时仍可生成发布包，但 Windows 可能显示“未知发布者”或 SmartScreen 风险提示。
- 如后续配置 OV/EV 证书或 Microsoft Artifact Signing，发布流程会自动签名；普通 OV 新证书仍可能经历 SmartScreen 信誉积累期。

### 便携版

发布文件：`dist/card-vault-1.0.17-portable.zip`

1. 完整解压 ZIP。
2. 运行解压目录中的 `Card Vault.exe`。
3. 不要只移动 EXE；程序依赖同目录下的完整运行文件。

便携版适合临时使用、测试、移动硬盘和快速分发。它不是“单文件绿色软件”，ZIP 解压后才是完整运行目录。

### 本地开发

首次准备：

```bash
npm ci
npm run db:init
npm run build
```

Windows 桌面启动：

```bat
start-desktop.bat
```

也可以运行：

```bash
npm run electron
```

开发版使用 `%APPDATA%\Card Vault Development` 保存桌面配置。首次切换时只继承旧开发配置中的数据路径和 AI 设置，不移动或清洗卡片数据。

## 数据与备份

Card Vault 的数据由 SQLite 数据库和媒体目录共同组成：

- `dev.db`：卡片、分享集和设置关联数据。
- `uploads`：卡片图片。
- `share-covers`：分享集封面。
- `share-backgrounds`：分享集背景。
- `schema-backups`：数据库迁移前快照。

设置页支持：

- 更改当前数据存储路径。
- 在“存储数据”中检查 SQLite 完整性、缺失文件和未引用文件。
- 查看未引用文件明细、在资源管理器中定位文件，并在二次确认后清理。
- 设置独立备份路径并执行一键备份。
- 使用“备份恢复”恢复数据；可选择日期文件夹或其中的具体 data 文件夹，恢复前会自动创建当前数据的安全备份。
- 备份、恢复、健康检查和清理在独立存储进程执行；设置页会显示当前阶段与进度，桌面主界面可继续响应。

更换电脑或重装前，请使用设置页备份功能迁移完整数据，不要只复制数据库或只复制图片。详细说明见 [数据备份说明](./docs/data-backup-guide.md)。

## AI 功能

- 全局 AI 设置位于“设置 → AI 设置”，支持 Azure OpenAI、MiniMax 和“自定义设置”。
- AI 设置由识图录入、分享文案和组合分析共用。
- API Key 保存在本机用户配置目录，并通过 Windows `safeStorage` 加密，不写入 Git、数据库或发布包。
- 如果 Windows 环境无法解密旧密钥，程序会继续启动并保留 Endpoint、部署和模型；请重新输入一次 API Key 后保存。
- AI 识图支持 1 至 2 张正反面 `jpg/png/webp` 图片，默认只填充空字段。
- 组合分析仅发送当前筛选结果的统计摘要，结果用于收藏整理参考，不构成投资或交易建议。
- AI 服务无法连接时会区分本地服务问题和上游网络、Endpoint、代理或服务商错误。
- Azure OpenAI 统一使用 `v1` API；Endpoint 可填写资源根地址（`.openai.azure.com` 或 `.services.ai.azure.com`），Deployment 填写 Azure 中实际创建的部署名称。
- “自定义设置”可新增并保存多套 OpenAI Chat Completions 兼容服务，每套配置拥有独立名称、聊天 Endpoint、模型列表 Endpoint、模型 ID 和加密 API Key；服务列表直接显示用户设定的名称。API Key 默认通过 `Authorization: Bearer` 发送，也可留空以支持本地无鉴权服务。AI 识图需要所选模型支持图片输入。
- GPT-5.4 / GPT-5.5 / GPT-5.6 请求统一使用 `/openai/v1/chat/completions`，不再保留日期式 `api-version` 配置；GPT-5 reasoning 模型请求不会发送不受支持的 `temperature` 参数。

## 分享展馆

- 分享集与本地展示页相互独立，只包含用户主动选择的卡片。
- 编辑器 2.0 将标题与介绍、视觉设计、展馆章节和单卡展示拆分为四个工作区，并保留随编辑同步更新的预览。
- 实时预览支持桌面和手机宽度切换；预览、静态包和 Cloudflare Drop 临时发布包继续共用同一渲染器。
- 可编辑标题、介绍、叙事、章节、主题、布局、封面、背景和单卡展示覆盖。
- 内置通用、运动和 Team 主题，预览、静态包和 Cloudflare Drop 临时发布包使用同一套渲染结果。
- 静态导出采用公开字段白名单，不包含价格、成本、购买渠道、私人备注、AI Key 或本地路径。
- Cloudflare Drop 临时发布包提供断链、私密字段、文件数量和单文件大小检查，并附带 noindex、404、内容清单和一小时临时发布说明。
- Card Vault 不记录 Drop 临时 URL 或认领链接；永久发布、更新、撤回和线上验证留待正式托管接入后实现。

编辑器 2.0 的边界、当前实现与后续阶段见 [分享展馆编辑器 2.0 说明](./docs/share-editor-2.0.md)。
Cloudflare Drop 的发布边界和包内检查见 [Cloudflare Drop 临时发布说明](./docs/cloudflare-drop-publishing.md)。
历史表结构、约束与旧数据迁移策略见 [财务历史数据模型](./docs/financial-history-model.md)。

## 财务历史

- 新增卡片时，“初始财务记录”会分别建立购买交易、评级费用和初始估值。
- 卡片资料编辑不会覆盖财务历史；交易、费用与估值统一在卡片详情页维护。
- 详情页支持 CNY/USD 独立汇总、统一时间线、单条纠错和删除；估值来源限定为“个人估计”“近期成交”“平台报价”。
- 旧版财务字段仅作为尚未迁移流程的 CNY 兼容快照，由历史记录自动重新计算；首页总估值和组合分析都直接读取财务历史。
- 首页“总估值”针对当前筛选出来的全部卡片，每张只采用业务日期最新的一条估值；CNY/USD 统一采用相同字号的 ISO 代码格式显示，不会按收藏状态排除，也不会重复累计历史估值。
- 首页“组合分析”按 CNY/USD 分别汇总真实交易、费用和最新估值，展示活跃成本基础、净现金占用、可比未实现盈亏、估值时效及来源，不会隐含换算汇率或虚构已实现收益。
- 从备份恢复旧数据库时，会先在暂存目录执行全部待办迁移并通过完整性检查，再替换当前数据；因此旧备份中的估值来源也会统一迁移为“个人估计”。

## 常用命令

| 命令 | 用途 |
| --- | --- |
| `npm run build` | 生成 Next.js 生产构建。 |
| `npm run typecheck` | 执行 TypeScript 类型检查。 |
| `npm run check:encoding` | 检查 UTF-8 和常见中文乱码。 |
| `npm run check:metadata` | 检查版本、锁文件、README 和发布说明一致性。 |
| `npm test` | 运行核心自动化测试。 |
| `npm run test:card` | 验证卡片新增、上传、编辑和详情流程。 |
| `npm run test:share` | 验证分享新建、编辑保存、预览和导出流程。 |
| `npm run check:release` | 执行完整发布前检查，但不生成安装包。 |
| `npm run release:win` | 检查并生成安装版与便携 ZIP。 |
| `npm run verify:release-artifacts` | 验证现有安装包、便携 ZIP 和 SHA-256 清单。 |
| `npm run clean:cache` | 清理可重新生成的缓存和日志。 |

## Git 与目录说明

GitHub 主要同步源码、文档和配置，不应提交个人数据与本机构建环境。`.gitignore` 默认忽略：

- `.env` / `.env.local`：可能包含 API Key 和本机配置。
- `node_modules`：可由 `npm install` 重新生成，体积很大。
- `.next`：可由 `npm run build` 重新生成。
- `data`、本地数据库和上传图片：属于个人收藏数据。
- `logs` 和缓存：仅用于本机运行与排错。

`dist` 是否提交由仓库发布策略决定。更推荐将安装版和便携 ZIP 上传至 GitHub Releases，并让源码仓库保持精简。

Windows 代码签名是可选增强项，不再阻塞日常打包。PFX、证书存储/EV 和 Microsoft Artifact Signing 的配置方式见 [Windows 代码签名说明](./docs/windows-code-signing.md)。证书文件和密码不得写入仓库或 `.env`。

## 项目结构

- `app/`：页面、API 路由和 Server Actions。
- `components/`：表单、筛选、设置和展馆组件。
- `lib/`：数据库、AI、图片、统计和导出逻辑。
- `electron/`：桌面主进程、preload、存储与 AI 配置。
- `prisma/`：数据库结构。
- `scripts/`：迁移、检查、测试和发布脚本。
- `tests/`：业务规则、数据安全和导出回归测试。

## 后续方向

已确认的长期版本路线为：

1. `v1.1.0`：卡片录入工作台 2.0。
2. `v1.2.0`：持仓数量、部分出售与收益模型。
3. `v1.3.0`：收藏组合中心与趋势分析。
4. `v1.4.0`：分享展馆 3.0。
5. `v1.5.0`：批量数据与迁移中心。
6. `v1.6.0`：提醒与收藏计划。
7. `v2.0.0`：具备长期资源后提供可选在线发布与多设备同步。

各版本目标、范围边界、风险控制和共同验收标准见 [Card Vault 产品开发路线图](./docs/product-roadmap.md)。

## 技术栈

`Next.js 16`、`React 19`、`TypeScript`、`Prisma`、`SQLite`、`Electron` 和自定义 CSS
