# Card Vault 数据备份说明

适用版本：`1.0.11`。本说明同时适用于从较早版本升级或迁移到 `1.0.11` 的数据。

## 1. 程序数据保存在哪里

Card Vault 的数据主要由以下部分组成：

- `dev.db`：SQLite 数据库文件，用来保存卡片文字信息、字段内容和筛选数据。
- `uploads`：图片文件夹，用来保存你上传的卡片图片。
- `share-covers`：分享集自定义封面。
- `share-backgrounds`：分享集自定义背景。

默认情况下，桌面端程序的数据通常位于：

```text
C:\Users\<你的用户名>\AppData\Roaming\QL's card vault\data
```

如果你在设置页手动修改过“存储路径”，请以设置页显示的“存储路径”为准。

## 2. 是否可以直接用文件夹备份

可以。

当前版本支持在设置页配置独立的“备份路径”。“一键备份”会复制全部图片资源，并为正在使用的 SQLite 数据库生成一致性快照和执行完整性检查。也可以继续在退出程序后手动复制整个当前“存储路径”文件夹。

一键备份会保留当前数据目录中的其他内容，例如已经生成的分享导出文件；SQLite 临时日志文件不会作为数据库备份使用。

至少要包含：

- `dev.db`
- `uploads`
- `share-covers`
- `share-backgrounds`

如果只备份 `dev.db`，卡片文字信息仍在，但卡片图片、分享封面和分享背景会丢失。

## 3. 推荐备份步骤

### 方法一：设置页一键备份

1. 打开设置页的“备份”模块。
2. 确认或选择独立的备份路径。
3. 点击“一键备份”。
4. 等待程序返回备份完成位置。

该方式无需退出程序，并会检查备份数据库是否完整。

### 方法二：完整文件夹备份

1. 完全退出程序。
2. 打开设置页，确认当前“存储路径”。
3. 进入该路径。
4. 复制整个文件夹到你希望保存的位置。

例如：

```text
原路径：
C:\Users\qlint\AppData\Roaming\QL's card vault\data

备份到：
D:\CardVaultBackup\2026-03-31\data
```

### 方法三：压缩备份

1. 完全退出程序。
2. 找到当前“存储路径”。
3. 右键该文件夹。
4. 压缩成 `.zip` 或 `.rar`。

这种方式适合长期归档，或者传输到另一台电脑。

## 4. 如何在另一台电脑上恢复使用

### 方法一：设置页直接恢复

1. 打开设置页的“备份与恢复”模块。
2. 点击“备份恢复”。
3. 选择一键备份生成的日期文件夹，或直接选择其中包含 `dev.db`、`uploads`、`share-covers` 和 `share-backgrounds` 的具体 data 备份文件夹。日期文件夹中存在多次备份时，程序会自动使用最新的一份。
4. 确认恢复。程序会先为当前数据创建安全备份，再验证并替换数据目录。
5. 恢复成功后 Card Vault 会自动重新启动。

如果备份数据库未通过 SQLite 完整性检查，程序会拒绝恢复。恢复中途失败时，原数据目录会自动回滚。

### 方法二：先安装程序，再指定存储路径

1. 在另一台电脑上安装并运行程序。
2. 暂时不要录入新数据。
3. 将你备份好的数据文件夹复制到新电脑。
4. 打开设置页。
5. 点击“更改路径”。
6. 选择备份数据所在的文件夹。
7. 程序会使用该路径中的数据库和全部图片目录。

这是最推荐的做法，也更适合跨电脑迁移。

### 方法三：覆盖默认数据目录

1. 在新电脑先运行一次程序。
2. 关闭程序。
3. 找到新电脑默认数据目录。
4. 用备份中的 `dev.db`、`uploads`、`share-covers` 和 `share-backgrounds` 覆盖过去。
5. 重新启动程序。

这种方式也能使用，但不如方法一清晰。

## 5. 迁移注意事项

- 使用设置页“一键备份”时不需要关闭程序或手动进入数据目录。
- 手动复制或压缩备份前请先关闭程序。
- 恢复前也请先关闭程序。
- 不要只复制 `card vault.exe`。
- 程序本体和收藏数据是分开的两部分。
- 真正需要迁移的是“存储路径”里的数据文件，或设置页一键备份生成的日期备份文件夹。
- 如果你已经修改过存储路径，请以设置页显示的路径为准，不要只看默认目录。

## 6. 版本 1.0.3 数据兼容说明

从 `1.0.3` 开始，Card Vault 扩展了更多卡片字段，并删除了与“产品线”重复的“系列”字段。

升级或迁移旧数据时，程序会在初始化数据库时自动补充新增字段；如果旧数据库中存在“系列”数据，会优先合并到“产品线”。已有“产品线”内容不会被旧“系列”覆盖。

建议在升级前仍然先备份当前“存储路径”文件夹，尤其是 `dev.db` 和 `uploads`。

## 7. 版本 1.0.8 备份与迁移说明

- `1.0.8` 没有新增数据库业务字段，现有 `1.0.7` 数据可以直接继续使用。
- 更换存储路径时，程序会迁移 `dev.db`、`uploads`、`share-covers` 和 `share-backgrounds`，目标目录中已有同名文件不会被直接覆盖。
- 一键备份会通过 SQLite `VACUUM INTO` 创建一致性数据库快照，并对生成的 `dev.db` 执行完整性检查。
- 如果备份中途失败，程序会删除本次未完成的备份目录，避免把残缺内容误认为有效备份。
- 同一天重复备份时，会生成 `data-HHMMSS` 等独立目录，不覆盖当天已有备份。

升级到 `1.0.8` 前建议先保留一份旧版完整数据目录；升级并确认数据、图片、分享封面和分享背景均正常后，再按新的“一键备份”流程建立首份 `1.0.8` 备份。

## 8. 版本 1.0.11 数据检查、恢复与迁移快照

- 设置页“存储数据”模块中的“检查数据健康”会执行 SQLite 完整性检查，并统计数据库引用但本地缺失的图片，以及本地存在但未被数据库引用的文件。
- 健康检查本身只读取数据；检查后可以展开查看未引用文件的类型和相对路径，也可以在资源管理器中定位单个文件，不会自动删除或修改文件。
- “清理未引用文件”会弹出系统确认框，并在确认后重新检查数据库，只删除仍未被引用且位于媒体目录中的文件。清理无法撤销，建议先执行“一键备份”。
- “备份恢复”可以接受一键备份生成的日期文件夹或其中的具体 data 文件夹，并要求备份包含可正常读取的 `dev.db`；恢复前会在当前备份路径中自动保存正在使用的数据。
- 数据库升级使用 `SchemaMigration` 记录有序迁移。首次接管没有迁移记录的旧数据库时，会在当前数据目录的 `schema-backups` 文件夹中创建升级前 SQLite 快照。
- `schema-backups` 用于升级故障回退，不代替用户设置的日常独立备份路径。
