# Card Vault 数据备份说明

## 1. 程序数据保存在哪里

Card Vault 的数据主要由两部分组成：

- `dev.db`：SQLite 数据库文件，用来保存卡片文字信息、字段内容和筛选数据。
- `uploads`：图片文件夹，用来保存你上传的卡片图片。

默认情况下，桌面端程序的数据通常位于：

```text
C:\Users\<你的用户名>\AppData\Roaming\QL's card vault\data
```

如果你在设置页手动修改过“存储路径”，请以设置页显示的“存储路径”为准。

## 2. 是否可以直接用文件夹备份

可以。

当前版本支持在设置页配置独立的“备份路径”，并通过“一键备份”把当前 data 文件夹完整复制到备份路径下。也可以继续手动复制整个当前“存储路径”文件夹。

至少要包含：

- `dev.db`
- `uploads`

如果只备份 `dev.db` 而没有备份 `uploads`，卡片文字信息仍在，但图片会丢失。

## 3. 推荐备份步骤

### 方法一：完整文件夹备份

1. 完全退出程序。
2. 打开设置页，确认当前“存储路径”。
3. 进入该路径。
4. 复制整个文件夹到你希望保存的位置。

例如：

```text
原路径：
C:\Users\qlint\AppData\Roaming\QL's card vault\data

备份到：
D:\CardVaultBackup\2026-03-31\data
```

### 方法三：压缩备份

1. 完全退出程序。
2. 找到当前“存储路径”。
3. 右键该文件夹。
4. 压缩成 `.zip` 或 `.rar`。

这种方式适合长期归档，或者传输到另一台电脑。

## 4. 如何在另一台电脑上恢复使用

### 方法一：先安装程序，再指定存储路径

1. 在另一台电脑上安装并运行程序。
2. 暂时不要录入新数据。
3. 将你备份好的数据文件夹复制到新电脑。
4. 打开设置页。
5. 点击“更改路径”。
6. 选择备份数据所在的文件夹。
7. 程序会使用该路径中的 `dev.db` 和 `uploads`。

这是最推荐的做法，也更适合跨电脑迁移。

### 方法二：覆盖默认数据目录

1. 在新电脑先运行一次程序。
2. 关闭程序。
3. 找到新电脑默认数据目录。
4. 用备份中的 `dev.db` 和 `uploads` 覆盖过去。
5. 重新启动程序。

这种方式也能使用，但不如方法一清晰。

## 5. 迁移注意事项

- 使用设置页“一键备份”时不需要手动进入数据目录。
- 手动复制或压缩备份前请先关闭程序。
- 恢复前也请先关闭程序。
- 不要只复制 `card vault.exe`。
- 程序本体和收藏数据是分开的两部分。
- 真正需要迁移的是“存储路径”里的数据文件，或设置页一键备份生成的日期备份文件夹。
- 如果你已经修改过存储路径，请以设置页显示的路径为准，不要只看默认目录。

## 6. 版本 1.0.3 数据兼容说明

从 `1.0.3` 开始，Card Vault 扩展了更多卡片字段，并删除了与“产品线”重复的“系列”字段。

升级或迁移旧数据时，程序会在初始化数据库时自动补充新增字段；如果旧数据库中存在“系列”数据，会优先合并到“产品线”。已有“产品线”内容不会被旧“系列”覆盖。

建议在升级前仍然先备份当前“存储路径”文件夹，尤其是 `dev.db` 和 `uploads`。
