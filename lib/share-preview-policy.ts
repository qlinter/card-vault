// Preview HTML is generated internally and escapes all user-controlled text.
// Keeping the srcDoc frame same-origin lets the app CSP load local theme/media images.
export const sharePreviewSandboxPolicy = "allow-scripts allow-same-origin";
